// Infer types (Implicit Types)
// Definition: When TypeScript infers the type of a variable based on its initial value.


let userName = "Shafeeq";
userName=30
let subscribers = 30_000;

let adminName = "John"; // TypeScript infers this as type 'string'
let age = 25; // Inferred as type 'number'
